# Example secrets for Streamlit Cloud or local `.streamlit/secrets.toml` (DO NOT commit real keys)
# Copy this file to `.streamlit/secrets.toml` locally for testing, or paste keys into the Streamlit Cloud Secrets UI.

# LLM / API keys (if your app uses them)
OPENAI_API_KEY = "your-openai-key-here"
GOOGLE_API_KEY = "your-google-api-key-here"
HUGGINGFACE_API_KEY = "your-huggingface-key-here"

# Optional: storage or DB connection strings
# For example, a simple file store or a small database used for persistence
# STORAGE_URL = "https://..."
# DATABASE_URL = "postgresql://user:pass@host:5432/dbname"
